using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//UI系の部品を使う際に必要
using UnityEngine.UI;


public class ButtonManager : MonoBehaviour
{
    private void Start()
    {

    }

    private void Update()
    {

    }

    public void OnClick(int num)
    {
        switch (num)
        {
            //押されたボタンのUnity側で設定された引数が1のだったとき
            case 1:

                break;
            //押されたボタンのUnity側で設定された引数が2のだったとき
            case 2: 

                break;
            //押されたボタンのUnity側で設定された引数が3のだったとき
            case 3:

                break;
            //押されたボタンのUnity側で設定された引数が4のだったとき
            case 4: 

                break;
            //押されたボタンのUnity側で設定された引数が5のだったとき
            case 5:

                break;
            //押されたボタンのUnity側で設定された引数が6のだったとき
            case 6:

                break;
            //押されたボタンのUnity側で設定された引数が7のだったとき
            case 7:

                break;
            //押されたボタンのUnity側で設定された引数が8のだったとき
            case 8:

                break;
            //押されたボタンのUnity側で設定された引数が9のだったとき
            case 9:

                break;
        }
    }
}
